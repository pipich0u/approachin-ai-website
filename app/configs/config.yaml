log:
  dir: "logs"
  file: "kvcache_app.log"
  #log level: debug, info, warn, error, crit
  level: "debug"
  backup_count: -1

db:
  type: "sqllite"
  database: "KVcache.db"
  host: "./"
  pool_size: 10